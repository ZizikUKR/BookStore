@using WebUI.Models
@model BookListViewModel


@foreach (var book in Model.Books)
{
    
@Html.Partial("_BookSharedView",book) 
}

<div class="btn-group pull-right">
    @Html.PageLinks(Model.PaggingInfo, x=>Url.Action("List",new { page=x,genre=Model.CurrentGenre}))
</div>

